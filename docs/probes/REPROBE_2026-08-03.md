# REPROBE_2026-08-03.md — scheduled 30-day drift check (Air → Pro handoff)

> **EXECUTED 2026-08-03, second attempt — COMPLETE, 8/8 no drift.** Authoritative record: `../FINDINGS.md` "2026-08-03 (later)" entry. First attempt aborted at the sanity row (`SESSION_2026-08-03_probe_aborted.md`); the completed run used a v2 runsheet with three corrections to this doc: (1) rows 1/3 needed `additionalDirectories: ["/tmp"]` in the probe settings (path scope preempts verb ALLOW — this doc's ALLOW expectations were written assuming user-level trusted roots that `--setting-sources project` strips); (2) row 14's command was malformed (redirect belongs on the heredoc *open* line — fixed here and in TEST_PLAN.md); (3) a deny-canary load-proof row (`rev .claude/settings.json` → auto-DENY) now precedes everything, because a PROMPT on a sanity row cannot otherwise distinguish "settings never loaded" from path scope. `MATCHER_LAST_VERIFIED` bumped to 2026-08-03. Kept in probes/ per the RESULTS_2026-06-01.md precedent.

Handoff from an Air session (2026-08-03) to a Pro session where Dan has dual monitors and can more comfortably drive the paste-and-report loop. Read this whole doc before doing anything.

## Why now

- `MATCHER_LAST_VERIFIED = 2026-06-05` in `update_claude_permissions.py:56` — **59 days stale** vs. 30-day threshold. Nag mechanism (Plan 01 Open Q 4) is firing on every `install.sh` run.
- `MATCHER_NAG_SNOOZE_UNTIL = "2026-07-25"` — expired 9 days ago, so the yellow nag is live.
- No matcher-shape investigation has run since the 2026-07-28 process_substitution finding (that was an incoming-weirdo triage, not a scheduled drift check).
- Family-3 has grown to six nodes since the June re-probe (`{file_redirect, pipeline, simple_expansion, brace_expansion, string, process_substitution}`); this pass confirms whether the June-era pattern still holds across the current row set.

## Pre-flight

1. Standard Nori sync-main should already have happened. Confirm the Air-side commit `bb82999` (probes settings.json add of `Bash(diff *)`, `Bash(find *)`, `Bash(grep *)`) is present: `git -C /Users/dan/code/dotfiles log --oneline -5`.
2. `claude --version` — record it for the FINDINGS entry (matcher behavior is version-scoped).
3. `hostname` should print `Dans-MacBook-Pro.local`.

## Protocol

Mode B. In a **separate terminal** (not the coordinating session):

```
cd /Users/dan/code/dotfiles/docs/active/chain-hook-maintenance/probes
claude --setting-sources project
```

That fresh Claude loads ONLY `probes/.claude/settings.json` — no user-level hooks (`block_bash_chains.py` etc. inert), no user-level allow rules. Auth still works. Sanity-check by asking the probe Claude to run `pwd` and `ls settings.json` — both should succeed silently.

The coordinating session (you, reading this) drives:
- Paste each User message below one at a time into the probe Claude.
- Watch: prompt appears (**PROMPT**) or the tool runs (**ALLOW**). Deny each PROMPT after noting it.
- For PROMPTs on the Family-3 rows (14, 16, 17), capture the matcher's reason-text **verbatim** — that's the drift-sensitive signal (node names come straight from tree-sitter-bash).
- Ask Dan to confirm each result (`AskUserQuestion` — the canonical channel per METHODOLOGY step 4; comment-channel is unreliable in current CC).

## Minimum-viable test set (8 rows)

Full context and hypotheses in `TEST_PLAN.md`; these are the one-per-family drift representatives.

**Pre-step (once, in the probe session — required for test 21):**

> Use the Bash tool to run this exact command once and stop: `touch /tmp/x.py`

**Tests:**

| # | User message (paste verbatim) | Expected | Family / notes |
|---|---|---|---|
| **1** | Use the Bash tool to run this exact command once and stop: `touch /tmp/probe_sanity` | ALLOW | sanity |
| **3** | Use the Bash tool to run this exact command once and stop: `mkdir -p /tmp/probe3 && touch /tmp/probe3/x` | ALLOW | per-segment (both segments allow-listed) |
| **4** | Use the Bash tool to run this exact command once and stop: `mkdir -p /tmp/probe4 && unknownmarker_test4` | PROMPT | per-segment (seg2 unknown) |
| **14** | Use the Bash tool to run this exact command once and stop: `python3 <<'PY' 2>&1`<br>`print(1)`<br>`PY` | PROMPT — `file_redirect` | Family-3 (heredoc + redirect). *Corrected 2026-08-03: redirect must be on the OPEN line; the original form (`PY 2>&1` on the terminator line) is heredoc body text, parses as no redirect, and correctly ALLOWs.* |
| **16** | Use the Bash tool to run this exact command once and stop: `ls /tmp/{a,b}` | PROMPT — "Brace expansion" | Family-3 row #2 |
| **17** | Use the Bash tool to run this exact command once and stop: `diff <(echo a) <(echo b)` | PROMPT — `process_substitution` | Family-3, newest (2026-07-28); most drift-sensitive |
| **20** | Use the Bash tool to run this exact command once and stop: `find /Users/dan -maxdepth 1 -name .bashrc` | PROMPT | Family-2 (find ancestor of cwd, outside trusted roots) |
| **21** | Use the Bash tool to run this exact command once and stop: `/tmp/x.py` | PROMPT | allow-rule gap (`.py` verb) |

## Interpretation

- **All 8 match expected** → no drift. Confirms the June-era model still holds. Proceed to "Post-probe (no drift)."
- **Any row deviates** → drift. Do NOT auto-bump `MATCHER_LAST_VERIFIED`. Write a full FINDINGS entry documenting the deviation, escalate to Dan for hook re-design decisions per work-line thesis (frequent AND clean-alternative = deny hook candidate).

## Post-probe (no drift)

1. **Bump the verified date:** `update_claude_permissions.py:56` — change `MATCHER_LAST_VERIFIED = "2026-06-05"` → today's date.
2. **Clear the expired snooze:** `update_claude_permissions.py:64` — change `MATCHER_NAG_SNOOZE_UNTIL: Optional[str] = "2026-07-25"` → `None`. (It's expired; leaving it set is misleading.)
3. **Run the test trio** to confirm the date change doesn't break anything:
   ```
   pytest tests/test_sync_to_basic_config.py tests/test_update_claude_permissions.py claude-hooks/test_hooks_executable.py
   ```
4. **Write a brief FINDINGS entry** at top of `../FINDINGS.md`:
   - Header: `## 2026-08-03 — Scheduled 30-day drift check: <N>-of-8 confirms <June-era / 2026-07-28> model`
   - Methodology pointer (Mode B, `--setting-sources project`, this doc).
   - Results table (the 8 rows with ALLOW/PROMPT + captured reason-text where available).
   - Interpretation: "Model held" or specific deviations.
   - Impact on the hook: usually "None." Note the CC version bump if any.
   - Confidence: usually "Medium — 8 min-viable rows, one per family; a full pass would extend to Family-1 (test 18), the loop-body rows, and the speculatives."
5. **Move this doc:** `git mv REPROBE_2026-08-03.md ../results/` (or leave in probes/ — `RESULTS_2026-06-01.md` set the "results in probes/" precedent, but the results/ dir was added later; use your judgment).
6. **Commit + push:** scope the commit to `update_claude_permissions.py`, `FINDINGS.md`, and this runbook's move. Do NOT amend the settings.json commit (`bb82999`) — that's independent and already pushed.

## Post-probe (drift found)

1. **Do NOT bump `MATCHER_LAST_VERIFIED`.** The whole point of the constant is that it records the last honest positive-data point.
2. Consider snoozing the nag: set `MATCHER_NAG_SNOOZE_UNTIL` to a date reflecting when you expect to investigate (e.g., today + 3 days).
3. **Write a full FINDINGS entry** documenting the deviation. Include the row that broke, the observed vs. expected reason-text, and the isolation matrix you'd want to run next.
4. **Add an INCOMING.md entry** so future sessions see it.
5. If the deviation suggests a hook is over- or under-firing, that's a hook-redesign candidate — work-line thesis applies (frequent AND clean-alternative → Strategy 2 deny hook; else Strategy 0 with a documented sketch).

## Cross-machine note

Air-side session ran only steps: (a) added missing allow rules to `probes/.claude/settings.json` (`Bash(diff *)`, `Bash(find *)`, `Bash(grep *)` — commit `bb82999`); (b) wrote this handoff doc. No FINDINGS or `MATCHER_LAST_VERIFIED` changes were made on Air. The actual probe pass and constant bump happen on Pro.
